node_modules
.next
*.tsbuildinfo
# Skill content (categories/) is vendored into this repo - see
# scripts/build-skills.ts for the build pipeline. To re-sync from upstream,
# run `npm run sync:skills` (TODO) or delete categories/ and re-hydrate.

out
.vercel
.env*.local
.DS_Store
*.log
data/stats.local.json
content/skills/
content/tarballs/
.turbo
